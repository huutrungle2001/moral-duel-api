# FastAPI and ASGI Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
prisma==0.15.0
redis==5.0.1
aioredis==2.0.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
bcrypt==4.1.2

# Neo Blockchain
neo-mamba>=2.7.0
neo3-boa>=1.3.0

# AI Services (via Spoon AI with Google Gemini)
# Note: Spoon AI SDK must be installed from source:
# git clone https://github.com/XSpoonAi/spoon-core.git /tmp/spoon-core
# cd /tmp/spoon-core && sed -i '' 's/neo-mamba>=3.0.1/neo-mamba>=2.7.0/' pyproject.toml
# pip install -e /tmp/spoon-core
# Alternatively: git+https://github.com/XSpoonAi/spoon-core.git (after patching)

# Background Jobs
apscheduler==3.10.4
celery==5.3.6

# Utilities
python-dotenv==1.0.0
httpx==0.26.0
aiofiles==23.2.1

# Logging and Monitoring
python-json-logger==2.0.7
sentry-sdk[fastapi]==1.40.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0
